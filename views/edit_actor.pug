extends master.pug
block content
    form#myForm(action='/api/actor')
        input(type="hidden" name="id" value=_id)
        div
            p= name
            label(for="name") Name
            input(name="name" value=name)
        div
            label(for="age") Age
            input(name="age" value=age)
        div
            label(for="country") Country
            input(name="country" value=country)
        div
            input(type="submit" value="Submit")
block scripts
    script
        include form.js
